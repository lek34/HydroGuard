<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/navigation_home">

    <fragment
        android:id="@+id/navigation_home"
        android:name="com.example.hydroguard.Home"
        android:label="@string/title_home"
        tools:layout="@layout/fragment_home" />

    <fragment
        android:id="@+id/navigation_todolist"
        android:name="com.example.hydroguard.ToDoList"
        android:label="ToDoList"
        tools:layout="@layout/fragment_to_do_list" />

    <fragment
        android:id="@+id/navigation_history"
        android:name="com.example.hydroguard.History"
        android:label="History"
        tools:layout="@layout/fragment_history" />

    <fragment
        android:id="@+id/navigation_profile"
        android:name="com.example.hydroguard.Profile"
        android:label="History"
        tools:layout="@layout/fragment_profile" />

</navigation>